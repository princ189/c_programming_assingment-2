#include <stdio.h>

int main() {
    printf("The first 10 odd numbers are:\n");
    for (int i = 1, count = 0; count < 10; i += 2) {
        printf("%d ", i);
        count++;
    }
    printf("\n");
    return 0;
}
